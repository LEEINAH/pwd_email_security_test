server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/email_security?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    default: naver # ?? ??? google? ?? (naver? ?? ??)
    accounts:
      google:
        host: smtp.gmail.com
        port: 587
        username: 22na7592@gmail.com
        password: your-google-app-password
        properties:
          mail.smtp.auth: true
          mail.smtp.starttls.enable: true
      naver:
        host: smtp.naver.com
        port: 587
        username: care-point@naver.com
        password: carepoint@@
        properties:
          mail.smtp.auth: true
          mail.smtp.starttls.enable: true

  mybatis:
    mapper-locations: classpath:mapper/*.xml
    configuration:
      map-underscore-to-camel-case: true